const express = require('express'); // Imports Express.js library
const fetch = require('node-fetch'); // Imports a library that allows your Node.js server to make HTTP requests to other servers (like OpenWeatherMap's API for example)
require('dotenv').config(); // Loads the environment variables from the .env file into the process.env object

